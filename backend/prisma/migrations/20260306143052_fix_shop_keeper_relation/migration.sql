BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[Shop] DROP CONSTRAINT [Shop_keeperId_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
